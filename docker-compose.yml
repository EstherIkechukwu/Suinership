version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: billStation_db
      POSTGRES_USER: sulaimonlawal
      POSTGRES_PASSWORD: Omolade3
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=billStation_db
      - DB_USER=sulaimonlawal
      - DB_PASSWORD=Omolade3
      - REDIS_HOST=127.0.0.1
      - REDIS_PORT=6379
      - SECRET_KEY=bfky0g+dx7geln)tu0i-(t)vdeg^#l14gei)qnat@gt@6)=6se
      - ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
    depends_on:
      - db

volumes:
  postgres_data: